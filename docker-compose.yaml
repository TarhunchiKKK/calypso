version: "3.7"

services:
  boards-db:
    container_name: boards-db
    image: mongo:latest
    environment:
        - MONGO_INITDB_DATABASE=${BOARDS_DB_NAME}
    ports:
        - ${BOARDS_DB_PORT}:27017

 