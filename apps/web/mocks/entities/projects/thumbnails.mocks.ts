const thumbnailsCount = 20;

export const MockProjectThumbnails = Array.from({ length: thumbnailsCount }).map(
    (_, index) => `https://github.com/TarhunchiKKK/calypso/blob/main/assets/project-thumbnails/${index}.svg`
);
