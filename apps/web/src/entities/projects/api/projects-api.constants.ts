import type { DuplicateProjectDto, Id, UpdateProjectDto } from "@repo/common";

// TODO: implement
export const ProjectsApi = {
    updateOne: async (projectId: Id, dto: UpdateProjectDto) => {},
    duplicate: async (dto: DuplicateProjectDto) => {}
};
