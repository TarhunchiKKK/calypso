import type { ProjectFilters } from "@lib/projects";

export const DefaultProjectFilters: ProjectFilters = {
    type: undefined,
    own: undefined,
    sortOrder: "alphabetic"
};

export const DEFAULT_PROJECTS_QUERY_COUNT = 10;
